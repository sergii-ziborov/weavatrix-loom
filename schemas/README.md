# Schemas

JSON schemas for WVX wire documents will live here as the `0.1` format stabilizes.

Until then, treat `wvx-ir` Rust types as the source of truth and the fixture files
under `fixtures/` as examples.
